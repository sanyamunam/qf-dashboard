# §12 acceptance tests — run 2026-08-11, against the live build

**The ninety-second test.** Without scrolling, L1 shows: what's going right (Eco-Schools scale, research climb, WISE prize complete, DIFI revenue up), what's going wrong (WISH reach 900 vs 11,939, the only movement of that magnitude), and what to ask (two named questions with owners). The verdict is the headline; the chips are footnotes. PASS.

**The deletion test.** Delete every chart: The Standing, card sentences, card questions, AI lines and the theme reads still carry the full finding set — the platform still informs. Delete every AI sentence: what remains is cards of numbers and charts, i.e. Release 1. PASS both directions.

**The trust test.** Every Standing figure is an evidence link that opens the KPI drawer; the drawer cites sheet row numbers; every AI utterance carries a stated comparison basis and a confidence line with its reason; both L2 and L3 carry an explicit "what I can't see" line. Ask-bar claims spot-checked against cells (15 exact hits verified, including LinkedIn 7,000/7,000, testbeds 9/9 and 7/7). PASS.

**The greyscale test.** No finding is carried by colour alone: states are written words everywhere, the anomaly card is distinguished by an outline + "Needs you" chip + its own written content, targets are dashed lines with text labels, and the dot grid uses filled-vs-hollow shapes. Colour reinforces (indigo theme hue, maroon reserved for a breach that currently never renders). PASS.

**The idle test.** Zero breaches renders as words, three times: the Standing footer ("Above ceiling 0 — nothing is breaching this quarter"), the OE banner tile ("Hard ceilings breached: 0"), and the OE theme read ("The engine room is quiet, and quiet is what it is for"). Idle KPIs render "Idle this cycle — the target is 0 in an off year. Correctly quiet." The loader's computed third line says "Nothing needs you today" when the findings engine returns none. PASS.

## Data-trap checks (§3.3)
- No "% of annual target" anywhere at Q1 without pace framing; in-progress KPIs show "N of M", never a percentage.
- 74 zero Q1 actuals: annual KPIs' Q1 zero-cells treated as artifacts (excluded from movement, replaced by last annual reading in tiles); cyclical zeros rendered as idle-by-design.
- No two-point line exists: <3 readings renders the first-reading state, everywhere, including the drawer.
- `NR`/`NA` render as words, never 0. Parse warnings: 0.
- All 9 Polarity-Red KPIs annual → breach path built, renders empty, stated as the truth.
- Overachievement (Policy Hub Multiversity 11 vs 5, WISH partnerships 4 vs 3 / 6 vs 3) flagged as target-setting questions.

## Declared deviations
- QF/Aeonik typefaces unlicensed → Instrument Sans (UI) + Fraunces (AI voice, the declared serif risk). Space Grotesk numbers per brief.
- BOTaina GIF not on disk → inline SVG recreation with six states.
- QF public theme hues not extractable from compiled CSS → brief's proposed hues, AA-darkened for small text.
- No `Parent Entity` column exists in the ALL sheet — the brief's schema lists one; routing uses entity names.
- Loader counter lines run ~1.95s inside the 2.4s budget; "since you last looked" renders only on genuine state delta (so a fresh demo shows none — honest by design).
